apiVersion: v1
kind: PersistentVolume
metadata:
  name: face-recognition-pvc-unknown
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  volumeMode: Filesystem
  storageClassName: my-local-pvc
  local:
    path: /tmp/unknown_people
